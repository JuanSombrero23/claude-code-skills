# session-curator (plugin)

Cross-project Claude Code session browser, cleaner, renamer, mover, and live monitor.

Plugin #1 of the [`claude-code-skills`](../../README.md) marketplace. For install and usage, see the [root README](../../README.md). The skill itself lives in [`skills/session-curator/`](skills/session-curator/).

Requires **PowerShell 7+ (`pwsh`)**.
